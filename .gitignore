.env
__pycache__/
*.pyc
# data/seen_postings.json and data/dashboard_history.json are intentionally
# TRACKED (not ignored): the GitHub Actions workflow commits them back after
# each run so dedup and dashboard history persist across runs on ephemeral
# runners. See .github/workflows/schedule.yml.
data/post_history.json
data/digests/
data/post_drafts/
.venv/
venv/
